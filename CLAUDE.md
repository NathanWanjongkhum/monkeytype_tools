# Guidelines

## Data Fetching

The MonkeyType API is rate limited at 30 attempts every 24 hours. Since attempts are expensive use local data only (use `--offline`). If e2e requires fresh data for verification request it from the user.
